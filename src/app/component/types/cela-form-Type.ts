export interface IcelaAll{
    GeoHostingOwner: string;
    Scope: string;
    AssignedTo: string;
    // DueDate: string;  // should have
   //  Priority: string; // should have
   CompletedDate: string;
   RiskLevel: string;
   FeedbackSummary: string;
   // CPIRating; // should have
   // License Requirements for Terrestrial Fiber
   // License Requirements for Submarine Cables
   Summary: string;
   // Global Network Initiative (GNI)
}